{{- define "titan-mesh-helm-lib-chart.envoy.matcher-headers" }}
  {{- if .headers }}
  headers:
  {{ include "titan-mesh-helm-lib-chart.envoy.string.match" (dict "matches" .headers) }}
  {{- end }}
{{- end }}